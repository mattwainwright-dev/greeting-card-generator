# Greeting Card Generator

Enter a name, adjective, and noun to generate a card, with a hidden surprise after five cards.

## How it's built

- `index.html` + `card.js` — the greeting card generator page and its JavaScript.
- `scripts/` — practice JavaScript files from earlier parts of the project.

## Live

- GitHub Pages URL coming next.